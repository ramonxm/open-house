import { createTheme } from '../../stitches.config';

export const darkTheme = createTheme({
  colors: {
    dark: 'hsl(206,10%,5%)',
  },
});

export const lightTheme = createTheme({
  colors: {
    light: 'hsl(206,10%,100%)',
  },
});
